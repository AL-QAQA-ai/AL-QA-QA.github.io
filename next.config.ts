import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  trailingSlash: true,
  basePath: "/AL-QA-QA.github.io",
  assetPrefix: "/AL-QA-QA.github.io",
};

export default nextConfig;
